// import PropTypes from 'prop-types';

const Head = ({ pageTitle }) => {
    return (
        <h1>{pageTitle}</h1>
    )
}

export default Head;

// Title.PropTypes = {
//     pageTitle: PropTypes.string
// }